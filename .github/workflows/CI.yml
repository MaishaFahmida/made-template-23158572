name: CI

on:
  push:
    branches:
      - main
    paths:
      - "project/tests.sh"
      - "project/tests.py"
      - "project/pipeline.py"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.12

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas requests pytest sqlalchemy

    - name: Ensure database directory exists
      run: |
        mkdir -p ./data
        touch ./data/Final_Data.db

    - name: Make scripts executable
      run: |
        chmod +x ./project/tests.sh
        chmod +x ./project/pipeline.py

    - name: Run pipeline
      run: ./project/pipeline.py

    - name: Run tests
      run: python3 ./project/tests.py
